{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run teaBASE (Debug)",
            "type": "node-terminal",
            "request": "launch",
            "preLaunchTask": "teaBASE: build",
            "command": "${workspaceFolder}/DerivedData/Build/Products/Debug/teaRUN.app/Contents/MacOS/teaRUN",
            "cwd": "${workspaceFolder}",
            "internalConsoleOptions": "neverOpen"
        }
    ]
}
